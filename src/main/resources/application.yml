server.port: 8085
spring:
  jpa:
    hibernate.ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
  datasource.url: jdbc:mysql://localhost:3306/online_booking?serverTimezone=Europe/Bucharest
  datasource.username: online_booking
  datasource.password: online_booking
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration